
error: cannot find symbol
  symbol:   method accrueInterestAllAccs()
  location: variable bank of type Bank

--------------------------------------------------------------------------------------------------------------------------------
Ans 

{"answer": The error message \"cannot find symbol\" in Java usually occurs when a method or variable is not properly declared or imported. In this case, the issue seems to be with the `accrueInterestAllAccs()` method being called on an object of type `Bank`, but it cannot find that method because it is not declared or imported correctly.\n\nTo resolve this error, you can try the following:\n\n1. Make sure that the `accrueInterestAllAccs()` method is properly declared and defined in the `Bank` class.\n2. Import the necessary classes and packages that contain the `Bank` and `Transaction` classes if they are not already imported.\n3. Check that the variable `bank` is properly initialized and refers to an object of type `Bank`.\n4. If none of the above steps solve the issue, try cleaning and rebuilding the project to ensure that all errors are properly resolved.\n\n\ud83d\udcd6


myTrans = new Withdrawal (accountId,pin,balance,transNum,transType,amount);
data[numAccts] = myTrans;
numAccts ++;
}catch(InputMismatchException ex){
System.out.println("Incorrect input type.");
System.exit(0);
}
}else {
System.out.println("Invalid transcation type");
System.exit(0);
}
}
inputStream.close();
}catch (FileNotFoundException e){
System.out.println("File does not exist.");
System.exit(0);
}
sortArray();
}

public void printTransactions( ){
for(int i = 0; i < numAccts; i++){
if(data[i] != null){
System.out.println(data[i]+" ");
}
}
}

private void sortArray(){
for (int i = 0; i<data.length-1;i++){
if(data[i] != null){
int currentMin = data[i].getTransNum();
int currentMinIndex = i;
for (int j = i+1; j<numAccts;j++){
if (currentMin > data[j].getTransNum()){
currentMin = data[j].getTransNum();
currentMinIndex = j;
}
}
if (currentMinIndex != i){
Transaction temp=data[currentMinIndex];
data[currentMinIndex]=data[i];
data[i]=temp;
}
}
}
}

Context:
private void point() {
//this method is activated when the "." button is pressed. Along with
//the newdigit method, it helps accurately place numbers following a
//decimal point
startNew();
decimals = 1;
}

private void newDigit(int n) {
//this method will add a number onto the end of the accumulator, it will
//multiply the value by ten if it is not a decimal. If it is a decimal,
//it will add numbers appropriately to the left of the point
startNew();
if (decimals == 0){
accumulator *= 10;
}
accumulator += (n * Math.pow(10, -decimals));
display();
if (decimals >= 1){
decimals++;
}
}
private void startNew(){
//if it is a new number, then accumulator and decimals will be initialized
//and the startNew boolean becomes false until the field is cleared
if (startNew){
startNew = false;
accumulator = 0;
decimals = 0;
}
}
private void clear() {
//this clears the stack, initializes the accumulator and decimals variables
//, sets startNew flag to true, and calls display (which displays a 0)
stack.empty();
accumulator = 0;
decimals = 0;
startNew = true;
display();
}

public Calculator() {
initComponents();
}

/**

    This method is called from within the constructor to initialize the form.

    WARNING: Do NOT modify this code. The content of this method is always

    regenerated by the Form Editor.
    */
    //

    // Add components here

}//
}

📖 Source Docs:

--- Document 1 ---
myTrans = new Withdrawal (accountId,pin,balance,transNum,transType,amount);
data[numAccts] = myTrans;
numAccts ++;
}catch(InputMismatchException ex){
System.out.println("Incorrect input type.");
System.exit(0);
}
}
else {
System.out.println("Invalid transcation type");
System.exit(0);
}
}

inputStream.close();     
    }catch (FileNotFoundException e){
      System.out.println("Fi...

--- Document 2 ---
private void point() {
//this method is activated when the "." button is pressed. Along with
//the newdigit method, it helps accurately place numbers following a
//decimal point
startNew();
decimals = 1;
}

private void newDigit(int n) {
    //this method will add a number onto the end of the accumulator, it will
    //multiply the value by ten if it is not a decimal. If it is a decimal,
    //it will add numbers appropriately to th...